apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prometheus
  namespace: observability
spec:
  interval: 5m
  chart:
    spec:
      chart: prometheus
      version: "25.0.0"
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system